import net.joinedminds.tools.evet.TimePlotViewBuilder

import java.text.DateFormat

TimePlotViewBuilder b = my

//DateFormat format = DateFormat.getDateInstance(DateFormat.MEDIUM)

div(id: "the-timeplot", style: "height: 96%; width: 99%; margin: 3px; border: 1px solid #aaa")

/*
table(border: 1, cellspacing: 0, cellpadding: 3) {
    tr {
        th("Day")
        th("Count")
    }
    b.data.each { Map.Entry<Date, Integer> row ->
        tr {
            td(format.format(row.key))
            td(row.value)
        }
    }
}*/
